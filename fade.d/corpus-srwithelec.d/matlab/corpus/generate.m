function generate(target_dir, samples, seed)

mkdir(target_dir);
classes = {0, 1};

% Tone-in-noise detection
funname = 'SpectralRipple';
offsets = [2 3 5 8 11 15 20]; % modutiefen
% conditions: erstes ist RPO, zweites dBLevel
generate_conditions(target_dir, funname, classes, ...
  num2cell(offsets), samples, {'0.25,-49'}, seed);
generate_conditions(target_dir, funname, classes, ...
  num2cell(offsets), samples, {'0.5,-49'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'1.5,-49'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'2.0,-49'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'2.5,-49'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'3.0,-49'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'0.25,-44'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'0.5,-44'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'0.25,-39'}, seed);
%generate_conditions(target_dir, funname, classes, ...
%  num2cell(offsets), samples, {'0.5,-39'}, seed);

end
